# Canonical Bestand + Workflows (T1-6a final) werden NICHT umformatiert
*.html
*.json
.github/
node_modules/
baseline/
schemas/
package-lock.json
# Apps-Script-Rechner-Backend: byte-genau verifizierter Spiegel des Codes, der im
# standalone Apps-Script-Projekt laeuft (Port von js/site.js, Tier-4 2026-06-17).
# NICHT umformatieren -> Repo-Kopie bleibt identisch zum deployten/verifizierten Stand.
apps-script/
# Vendor-Minifikat Chart.js 4.4.1 (UMD, 200.807 B in einer einzigen Zeile).
# Fremdcode, von keiner Aenderung angefasst. Umformatieren wuerde den Einzeiler
# in tausende Zeilen aufblasen, ohne dass ihn je jemand liest.
js/chart-4.4.1.umd.min.js
# Maschinell erzeugter Messbericht des Fluchten-Gates. Er wird bei jedem Lauf neu
# geschrieben; ihn zu formatieren wuerde das Gate gegen sich selbst laufen lassen.
# BEWUSST eng gefasst: reports/ enthaelt 13 handgeschriebene Dokumente, die
# weiter von Prettier geprueft werden sollen.
reports/*_Raster-Befund_HERO.md
# Maschinell erzeugter Messbericht des Mobile-Gates, gleiche Begruendung wie
# oben: er wird bei jedem Lauf neu geschrieben. Ebenfalls eng gefasst, damit die
# handgeschriebenen Berichte in reports/ weiter geprueft werden.
reports/*_Mobile-Verifikation_HERO.md
